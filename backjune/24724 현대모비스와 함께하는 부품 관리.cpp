#include <iostream>

using namespace std;

int main() {
	ios::sync_with_stdio(false);
	cin.tie(NULL);
	cout.tie(NULL);

	int T, x;
	x = 1;
	cin >> T;
	while (T--) {
		int N;
		cin >> N;
		int A, B;
		cin >> A >> B;
		while (N--) {
			int u, v;
			cin >> u >> v;

		}
		cout << "Material Management " << x << "\nClassification ---- End!\n";
		x++;
	}

}